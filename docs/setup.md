# Setting up a development server 
I recomend using pnpm
First clone repo
Secondly cd into repo
Thirdly run `pnpm install` (or `npm install` but pnpm works best)
Fourthly do `pnpm dev` (or `npm run dev`)
Fifthly enjoy!
